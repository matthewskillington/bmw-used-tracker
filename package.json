{
  "name": "your-project-name",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "tsc && cd dist && node index.js", 
    "start:gcloud": "tsc && cd dist && npx functions-framework --target=entry"
  },
  "type": "module",
  "types": "module",
  "dependencies": {
    "puppeteer": "^21.7.0"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
